- name: Deploy application
  hosts: app_servers
  become: true
  gather_facts: false

  tasks:
    - name: Install nginx
      apt:
        name: nginx
        state: present

    - name: Ensure nginx is running
      service:
        name: nginx
        state: started
        enabled: yes

